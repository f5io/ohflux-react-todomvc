# Keflux - Kefir + Flux in Reflux Style

- To install run `npm install`.
- Run with `npm start`.

## Roadmap

- Clean up stuff.
- Refactor `createStore`.
- Asynchronous actions.
- Extract lib.
- Document.
